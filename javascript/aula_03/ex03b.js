//3: Crie um algoritmo que decomponha uma palavra em seus caracteres e imprima os caracteres separadamente.

let palavra = 'cinema';

contador = 0;

while (contador < palavra.length){
    console.log(palavra[contador])
    contador += 1
};